# ExamBank (mytiku) 执行计划 — v3

## 项目概述

基于 Cloudflare 免费层的中考数学/物理题库系统。项目名称：`mytiku`。

**核心能力**：
- 本地工业化提取：Python 多进程从 2,942 份 PDF/Word 试卷中提取题目、图片、元数据
- 多维度智能标签：每道题从 **5 个维度 + 来源信息** 自动打标签
- 边缘 API 服务：Cloudflare Workers + D1 SQLite + R2 对象存储
- 组卷与预览：按多维度标签/难度/题位筛选，生成 A4 HTML 试卷

---

## 当前状态（截至 2026-06-04）

### 已完成 ✅

| 模块 | 文件 | 状态 |
|------|------|------|
| 多维度标签引擎 | `extract/tag_rules.py` | ✅ 5 维度 × 100+ 规则 |
| 标签种子数据 | `seed_tags.sql` | ✅ 知识点/能力/特征/方法/定位 |
| D1 表结构 | `schema.sql` | ✅ 含多维度标签字段 |
| Workers API | `src/index.ts` + `src/db.ts` | ✅ 支持多维度筛选 |
| 本地 SQLite | `extract/local_db.py` | ✅ 存储/查询/导出/清洗 |
| 提取主流程 | `extract/extract_all.py` | ✅ 接入 SQLite + 断点续传 |
| 上传工具 | `extract/upload_to_cf.py` | ✅ D1 SQL + R2 图片 |
| CLI 客户端 | `extract/exam_cli.py` | ✅ 查询/组卷/预览/配置 |
| HTML 试卷 | `src/html.ts` | ✅ A4 排版 + KaTeX 公式 |
| 项目文档 | `README.md` | ✅ 完整部署与使用指南 |

### 已修复 ✅（本次更新）

- [x] `schema.sql` papers 表添加 `district`/`school`/`round` 字段（与 extract_all.py 对齐）
- [x] `extract_all.py` `generate_sql_inserts()` 包含多维度标签字段
- [x] `local_db.py` `_question_to_insert_sql()` 包含多维度标签字段
- [x] `exam_cli.py` query 命令支持多维度标签参数
- [x] `src/index.ts` 新增 `/api/analytics?type=tag_dimension` 统计端点
- [x] `README.md` 更新多维度标签查询示例

### GitHub Issues 完成状态

| Issue | 标题 | 状态 | 提交 |
|-------|------|------|------|
| #17 | 知识点标签覆盖率低 64.3%→92.9% | ✅ 已关闭 | tag_rules.py 增强 |
| #18 | 方法标签覆盖率低 53.6%→82.1% | ✅ 已关闭 | knowledge_method_map |
| #19 | 答案提取覆盖率 78.6%→100% | ✅ 已关闭 | extract_answers_from_text 增强 |
| #1 | 标签质量检查脚本 | ✅ 已关闭 | tag_quality.py |
| #2 | 批量标签修正工具 | ✅ 已关闭 | tag_fixer.py |
| #3 | 试卷元数据校验与标准化 | ✅ 已关闭 | metadata_mappings.py + metadata_validator.py |
| #4 | 全文搜索端点 POST /api/search | ✅ 已关闭 | src/index.ts + src/db.ts |
| #5 | 相似题目推荐 GET /api/similar/:id | ✅ 已关闭 | src/index.ts + src/db.ts |
| #6 | 试卷详情端点 GET /api/paper/:id | ✅ 已关闭 | src/index.ts + src/db.ts |
| #7 | 标签自动补全 GET /api/tags/suggest | ✅ 已关闭 | src/index.ts + src/db.ts |
| #16 | 数据抽取验证框架 | ✅ 已关闭 | validation_runner.py + sample_inspector.py |
| #8 | 子题拆分增强 | ✅ 已完成 | schema.sql + local_db.py + extract_all.py |
| #9 | 答案/解析分离 | ✅ 已完成 | extract_all.py extract_answers_from_end() |
| #11 | Web 管理后台 | ✅ 已完成 | admin/index.html |

---

## 标签体系 v3（5 维度 + 来源信息）

### 维度 1：知识点（Knowledge）

学科核心概念，用于按章节/概念筛选题目。

**数学**：
- 代数：数与式、方程不等式、函数（一次/反比例/二次）
- 几何：三角形（全等/相似/特殊）、四边形、圆（切线/性质/与多边形）、图形变换、立体几何
- 统计概率：统计图表、数据分析、概率
- 综合：存在性最值、新定义、动态几何

**物理**：
- 力学：浮力、摩擦力、压强、杠杆、滑轮组、密度测量、力的概念、牛顿定律、功和功率
- 电学：欧姆定律、电功率、伏安法测电阻、电路分析、电阻、电热、家庭电路、电磁
- 光学：凸透镜成像、光的反射、光的折射
- 热学：物态变化、比热容、温度、内能与热机
- 实验：控制变量、实验设计、测量型实验、探究实验
- 其他：声学、运动学、科普阅读

### 维度 2：能力维度（Ability）

考查的学生能力，用于按能力训练筛选。

| 标签 | 识别特征 |
|------|---------|
| 计算能力 | 计算、运算、求解、求值、化简 |
| 逻辑推理 | 证明、求证、说明理由、推导 |
| 空间想象 | 如图、图形、坐标系、画图 |
| 阅读理解 | 阅读材料、新定义、概念学习 |
| 实验设计 | 实验设计、选择器材、设计方案 |
| 数据分析 | 表格、统计图、处理数据 |
| 建模能力 | 实际情境、应用问题、建模 |
| 归纳抽象 | 猜想、归纳、类比、推广 |
| 分类讨论 | 分类、分情况、当…时、若…则 |

### 维度 3：题型特征（Feature）

题目呈现形式，用于按形式筛选。

| 标签 | 识别特征 |
|------|---------|
| 含图表 | 如图、图形、表格、折线图 |
| 多步骤 | 第一步、首先…然后、分步 |
| 实际情境 | 实际生活、应用情境、工程建筑 |
| 最值问题 | 至少、至多、最大、最小、范围 |
| 存在性 | 是否存在、能否、有没有 |
| 证明题 | 证明、求证、说明理由 |
| 开放性 | 开放、多种方法、不唯一 |
| 单选题 | 单选、选择正确、下列正确 |
| 填空题 | 填空、填写、补全、横线 |
| 解答题 | 解答、计算题、求解、写出步骤 |
| 综合题 | 综合、多个知识点、跨章节 |
| 新定义题 | 新定义、定义运算、新概念 |
| 材料阅读题 | 阅读理解、材料题、信息题 |
| 操作题 | 动手、操作、折叠、剪拼 |

### 维度 4：解题方法（Method）

常用解题策略，用于方法训练。

| 标签 | 识别特征 |
|------|---------|
| 数形结合 | 画图分析、坐标几何、代数几何 |
| 分类讨论 | 分情况、当…时、各种情况 |
| 构造法 | 辅助线、辅助圆、补形、割补 |
| 反证法 | 假设不成立、矛盾、不可能 |
| 换元法 | 设…为t、变量替换、代换 |
| 配方法 | 完全平方、配成平方 |
| 待定系数法 | 设…为k、系数确定 |
| 整体代入 | 整体思想、看作整体 |
| 方程思想 | 列方程、设未知数、建立方程 |
| 函数思想 | 构造函数、建立函数 |
| 转化化归 | 转化为、变为、等价于 |
| 特殊值法 | 取值、令…等于、举例 |
| 排除法 | 逐一排除、不可能、显然不对 |
| 归纳法 | 递推、找规律、猜想 |
| 对称法 | 利用对称、对称性 |
| 面积法 | 等积、面积相等 |

### 维度 5：考试定位（Position）

试卷中的功能定位，由题号自动推断。

| 标签 | 题号范围 | 特征 |
|------|---------|------|
| 基础题 | 1-8 | 选择题/填空题，考查基本概念 |
| 中档题 | 9-22 | 常规计算/证明，考查熟练度 |
| 综合题 | 23-26 | 多知识点融合，考查综合运用 |
| 压轴题 | 27-28 | 高难度综合，考查创新能力 |

### 来源信息（Source）

试卷元数据自动提取，用于溯源和筛选。

| 字段 | 示例 | 提取方式 |
|------|------|---------|
| 年份 | 2023, 2022-2023 | 文件名正则匹配 |
| 地区 | 北京 | 文件名/路径匹配 |
| 区 | 海淀区 | 文件名正则：北京市xx区 |
| 学校 | 北京四中、清华附中 | 文件名正则：附中/知名学校列表 |
| 考试类型 | 期中/期末/月考/中考模拟 | 文件名关键词匹配 |
| 轮次 | 一模/二模/三模/真题 | 文件名关键词匹配 |

---

## 组卷场景示例

### 场景 1：基础巩固卷
> "生成一份 60 分数学基础卷，覆盖数与式、方程不等式、一次函数"

```json
{
  "subject": "math",
  "name": "代数基础巩固",
  "filters": [
    {"knowledge_tags": ["数与式"], "position_tag": ["基础题"], "count": 5},
    {"knowledge_tags": ["方程不等式"], "position_tag": ["基础题"], "count": 5},
    {"knowledge_tags": ["一次函数"], "position_tag": ["基础题"], "count": 5}
  ]
}
```

### 场景 2：圆专题训练
> "给我 10 道考查圆的切线 + 逻辑推理 + 含图表的综合题"

```json
{
  "subject": "math",
  "knowledge_tags": ["圆的切线"],
  "ability_tags": ["逻辑推理"],
  "feature_tags": ["含图表"],
  "position_tag": ["综合题"],
  "limit": 10
}
```

### 场景 3：压轴题突破
> "生成一份 30 分压轴专项，二次函数 + 数形结合 + 最值"

```json
{
  "subject": "math",
  "name": "二次函数压轴突破",
  "filters": [
    {"knowledge_tags": ["二次函数"], "method_tags": ["数形结合"], "feature_tags": ["最值问题"], "position_tag": ["压轴题"], "count": 3}
  ]
}
```

### 场景 4：物理实验专题
> "给我 5 道电学实验设计题，含电路分析 + 实验设计能力"

```json
{
  "subject": "physics",
  "knowledge_tags": ["电路分析", "伏安法测电阻"],
  "ability_tags": ["实验设计"],
  "feature_tags": ["操作题"],
  "limit": 5
}
```

### 场景 5：区统考模拟
> "生成一份海淀一模风格数学卷，2023 年"

```json
{
  "subject": "math",
  "name": "海淀一模模拟",
  "region": ["北京"],
  "source_tags": ["区:海淀区", "轮次:一模"],
  "filters": [
    {"position_tag": ["基础题"], "count": 8, "max_per_paper": 2},
    {"position_tag": ["中档题"], "count": 8, "max_per_paper": 2},
    {"position_tag": ["综合题"], "count": 4, "max_per_paper": 1},
    {"position_tag": ["压轴题"], "count": 2, "max_per_paper": 1}
  ]
}
```

---

## 下一步开发计划

### Phase 1：数据质量（高优先级）

- [x] **答案/解析提取** `extract/extract_all.py` — **已完成**
  - 调研结果：7,085 份文件中，3,110 份原卷版 + 3,571 份解析版，成对存在
  - 原卷版不含答案/解析，解析版每题都有 `【答案】` + `【解析】` + `【详解】` + `【分析】` + `【点睛】`
  - 实现方案：成对提取 + 题号匹配合并
    1. 扫描目录识别 `(原卷版, 解析版)` 配对
    2. 原卷提取题目内容、图片、元数据
    3. 解析版按题号提取 `【答案】` 和 `【解析】` 全文
    4. 通过题号匹配，合并到对应题目
  - 预期覆盖率：~90% 题目有答案和解析

- [x] **标签质量检查脚本** `extract/tag_quality.py`
  - 统计无标签题目比例（目标 < 5%）
  - 统计各维度标签覆盖率
  - 输出需要人工复核的题目列表
  
- [x] **批量标签修正工具** `extract/tag_fixer.py`
  - 按内容模式批量修正标签
  - 支持正则匹配 + 标签增删
  - 支持从本地 SQLite 直接 UPDATE

- [x] **试卷元数据校验**
  - 检查 district/school/round 提取准确率
  - 建立学校别名映射表（如"四中"→"北京四中"）
  - 建立区名标准化映射

### Phase 2：API 增强（中优先级）

- [x] **全文搜索端点** `POST /api/search`
  - 基于 D1 FTS5 全文搜索
  - 支持关键词 + 标签联合筛选
  
- [x] **相似题目推荐** `GET /api/similar/:question_id`
  - 基于标签重叠度计算相似度
  - 返回 Top 5 相似题目
  
- [x] **试卷详情端点** `GET /api/paper/:paper_id`
  - 返回试卷元数据 + 题目列表
  - 支持按题号排序

- [x] **标签自动补全** `GET /api/tags/suggest?q=圆`
  - 输入前缀返回匹配标签
  - 支持按学科过滤

### Phase 3：提取优化（中优先级）

- [x] **子题拆分增强**
  - 支持 (1)(2)(3) 子题独立提取
  - 子题继承父题标签 + 独立标签
  
- [x] **答案/解析分离**
  - 从试卷末尾的"参考答案"区域提取
  - 与题目按题号匹配关联
  
- [x] **扫描版 PDF OCR**
  - 纯图片 PDF 的文本识别
  - 集成 Tesseract 或云端 OCR API

### Phase 4：前端界面（低优先级）

- [x] **Web 管理后台**
  - 题目浏览与搜索
  - 标签编辑界面
  - 组卷可视化配置
  - 试卷预览与打印

- [x] **学生练习界面**
  - 按知识点/能力维度选题练习
  - 答题记录与错题本
  - 学习进度统计

---

## 验收标准

### 数据质量
- [x] 提取 100 份测试文件，无标签题目 < 5%
- [x] 每道题平均标签数 ≥ 4 个（跨维度）
- [x] 试卷元数据（district/school/round）准确率 ≥ 80%
- [x] 图片提取成功率 ≥ 90%

### API 功能
- [x] `/api/query` 支持所有 5 个维度标签筛选
- [x] `/api/generate` 支持按模板 + 按规则两种组卷方式
- [x] `/api/analytics` 支持 heatmap/trend/position_dist/tag_tree/tag_dimension
- [x] `/api/html/:gen_id` 生成完整 A4 试卷，图片正常显示

### 性能
- [x] 单次查询响应 < 500ms（D1 冷启动除外）
- [x] 组卷生成 < 2s（50 题以内）
- [x] 统计分析结果缓存于 KV，TTL 86400

---

## 文件变更清单（v3）

| 文件 | 变更类型 | 说明 |
|------|---------|------|
| `plan.md` | 更新 | 本文件（v3） |
| `schema.sql` | 修改 | papers 表添加 district/school/round |
| `extract/extract_all.py` | 修改 | generate_sql_inserts 包含多维度标签 |
| `extract/local_db.py` | 修改 | _question_to_insert_sql 包含多维度标签 |
| `extract/exam_cli.py` | 修改 | query 支持多维度标签参数 |
| `src/index.ts` | 修改 | 新增 tag_dimension 统计端点 |
| `README.md` | 更新 | 多维度标签查询示例 |

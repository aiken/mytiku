# GitHub Issues 更新建议

> 生成时间: 2026-06-05
> 由于当前环境无法直接调用 GitHub API（无 token），以下更新建议需手动执行

---

## #16 [Validation] 数据抽取验证 — 数学/物理各 100 套试卷

**状态**: open → 建议保持 open（验证完成，但发现待修复问题）

**建议评论内容**:

```
## 验证完成报告

### 执行摘要
- 验证集: 数学 100 套 + 物理 100 套 = 200 套
- 总题目数: 9,983 题
- 总试卷数: 200 份（全部成功处理）

### 验收标准检查结果

| 检查项 | 标准 | 实际 | 状态 |
|--------|------|------|------|
| 题目提取成功率 | ≥ 95% | 95.5% | ✅ 通过 |
| 无标签题目比例 | < 5% | 0.0% | ✅ 通过 |
| 每题平均标签数 | ≥ 4 个 | 8.7 | ✅ 通过 |
| 图片提取成功率 | ≥ 90% | 45.1% | ❌ 未通过 |
| 元数据准确率 | ≥ 80% | 83.3% | ✅ 通过 |
| 答案/解析匹配 | ≥ 90% | 0.0% | ❌ 未通过 |

### 发现的问题

1. **tag_rules.py TypeError** (已修复)
   - 问题: `question_number` 为字符串类型，传入 `infer_position_tag` 时触发 `TypeError: '<=' not supported between instances of 'str' and 'int'`
   - 影响: 导致 152/200 份试卷处理失败
   - 修复: 在 `auto_tag()` 函数开头添加 `int(question_number)` 转换
   - 提交: `3bd14f8`

2. **答案/解析覆盖率 0%** (需讨论)
   - 原因: 验证集全部为"原卷版"文件，不含答案/解析内容
   - 建议: 需要配对"解析版"文件或调整验收标准定义

3. **图片提取率 45.1%** (需讨论)
   - 原因: 大量题目（填空题、计算题、证明题）本身为纯文本，无图片
   - 建议: 指标定义需调整为"有图片的题目中成功提取的比例"

### 关联提交
- `73d2047` — 完善验证框架，支持 100 套抽样，自动验收指标计算
- `3bd14f8` — 修复 tag_rules.py 字符串题号 TypeError
```

---

## #12 [Frontend] 学生练习界面

**状态**: closed ✅（已由 GitHub 自动关闭或手动关闭）

**验证内容**:
- `practice/index.html` 已实现全部验收标准
- 支持知识点/能力/特征/方法维度选题
- 答题界面支持选择题/填空题/解答题
- 答题记录存储于 localStorage
- 错题本自动收录 + 手动删除
- 学习统计：总做题数、正确率、各维度正确率、练习时长
- 响应式设计（桌面 + 平板 + 手机）

**关联提交**: `673f80b`

---

## #5 [API] 相似题目推荐 /api/similar/:question_id

**状态**: open → 建议关闭

**说明**: 代码已在 `src/index.ts` (line 387) 和 `src/db.ts` (line 225) 完整实现，功能正常。

**关联提交**: 历史提交中已实现

---

## #6 [API] 试卷详情端点 /api/paper/:paper_id

**状态**: open → 建议关闭

**说明**: 代码已在 `src/index.ts` (line 390) 和 `src/db.ts` 完整实现，功能正常。

**关联提交**: 历史提交中已实现

---

## #7 [API] 标签自动补全 /api/tags/suggest

**状态**: open → 建议关闭

**说明**: 代码已在 `src/index.ts` (line 393) 和 `src/db.ts` (line 261) 完整实现，功能正常。

**关联提交**: 历史提交中已实现

---

## #10 [Extraction] 扫描版 PDF OCR 支持

**状态**: open → 保持 open（待开发）

**说明**: 尚未实现。需要:
- 集成 Tesseract OCR 或云端 OCR API
- PDF 类型检测（文本层 vs 纯图片）
- 中文识别准确率 ≥ 85%
- 与现有提取流程集成

---

## #13-#15 [Epic] 验收标准

**状态**: open → 保持 open

- #13 数据质量验收: 等待 #16 验证问题修复后更新
- #14 API 功能验收: #5/#6/#7 已实现，可推进验收
- #15 性能验收: 待进行压力测试
